info:
  name: Get a blog by id
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE}}/blog/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
