info:
  name: all live chats
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE}}/live-chat/all?page=0&limit=10"
  params:
    - name: page
      value: "0"
      type: query
    - name: limit
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
