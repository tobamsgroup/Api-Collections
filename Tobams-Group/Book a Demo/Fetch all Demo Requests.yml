info:
  name: Fetch all Demo Requests
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE}}/request-demo/all"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
