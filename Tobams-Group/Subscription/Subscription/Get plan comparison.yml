info:
  name: Get plan comparison
  type: http
  seq: 7

http:
  method: GET
  url: "{{BASE}}/subscriptions/plans/compare/:currentPlanId/:suggestedPlanId"
  params:
    - name: currentPlanId
      value: ""
      type: path
    - name: suggestedPlanId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
