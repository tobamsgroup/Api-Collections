info:
  name: Manage subscription add-ons
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{BASE}}/subscriptions/:subscriptionId/addons"
  params:
    - name: subscriptionId
      value: 688b3bd84ff5851455b2f831
      type: path
  body:
    type: json
    data: |-
      {
         "addOns": []
      }
  auth:
    type: bearer
    token: "{{org_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
