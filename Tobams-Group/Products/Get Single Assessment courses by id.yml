info:
  name: Get Single Assessment courses by id
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE}}/courses/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
