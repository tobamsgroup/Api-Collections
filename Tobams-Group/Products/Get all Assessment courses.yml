info:
  name: Get all Assessment courses
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE}}/courses"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
