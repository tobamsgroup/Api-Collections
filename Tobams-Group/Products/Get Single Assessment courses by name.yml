info:
  name: Get Single Assessment courses by name
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE}}/courses?name=career development"
  params:
    - name: name
      value: career development
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
