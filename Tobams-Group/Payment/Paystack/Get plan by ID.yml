info:
  name: Get plan by ID
  type: http
  seq: 6

http:
  method: GET
  url: "{{base}}/pay/plans/:planIDOrCode?paymentProvider=paystack"
  params:
    - name: paymentProvider
      value: paystack
      type: query
    - name: planIDOrCode
      value: PLN_ljzxd54qcrzk707
      type: path
  auth:
    type: bearer
    token: "{{org_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
