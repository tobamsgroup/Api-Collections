info:
  name: pause subscription
  type: http
  seq: 2

http:
  method: POST
  url: "{{URL}}/pay/paypal/pause-subscription"
  body:
    type: json
    data: "{\r

      \    \"subscriptionId\": \"\",\r

      \    \"reason\": \"No money for next payment\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
