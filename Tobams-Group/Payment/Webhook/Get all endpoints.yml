info:
  name: Get all endpoints
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE}}/webhooks"
  auth:
    type: bearer
    token: "{{admin_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
