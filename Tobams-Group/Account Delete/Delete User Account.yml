info:
  name: Delete User Account
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE}}/auth/delete-user"
  body:
    type: json
    data: |-
      {
        "email": "ona.newton@tobamsgroup.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
