info:
  name: Delete community -- (Admin Soft Delete)
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{BASE}}/communities/:communityId"
  params:
    - name: communityId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
