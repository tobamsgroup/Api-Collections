info:
  name: Get all community messages
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE}}/communities/67aa81104553b79bfe89a349/messages?page=1&limit=10"
  params:
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "10"
      type: query
  auth:
    type: bearer
    token: "{{intern_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
