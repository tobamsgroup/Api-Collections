info:
  name: Get all saved courses (bookmarks)
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE}}/talents/courses/bookmarks"
  auth:
    type: bearer
    token: "{{talent_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
