info:
  name: Bookmark (save) a course
  type: http
  seq: 1

http:
  method: PATCH
  url: "{{BASE}}/talents/courses/67acba8445825261ef08846a/bookmark"
  auth:
    type: bearer
    token: "{{talent_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
