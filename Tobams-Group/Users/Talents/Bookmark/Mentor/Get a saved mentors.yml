info:
  name: Get a saved mentors
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE}}/talents/mentors/bookmarks"
  auth:
    type: bearer
    token: "{{talent_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
