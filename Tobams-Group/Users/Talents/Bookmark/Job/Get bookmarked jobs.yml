info:
  name: Get bookmarked jobs
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE}}/talents/jobs/bookmarks"
  auth:
    type: bearer
    token: "{{talent_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
