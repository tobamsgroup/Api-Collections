info:
  name: Get current user
  type: http
  seq: 10

http:
  method: GET
  url: "{{BASE}}/talents/me"
  auth:
    type: bearer
    token: "{{talent_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
