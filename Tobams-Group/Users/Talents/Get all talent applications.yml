info:
  name: Get all talent applications
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE}}/talents/67b2124035275c7861ef1e5e/applications"
  auth:
    type: bearer
    token: "{{talent_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
