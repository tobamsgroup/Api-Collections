info:
  name: Filter
  type: http
  seq: 11

http:
  method: GET
  url: "{{BASE}}/user/all?user=talent"
  params:
    - name: user
      value: talent
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
