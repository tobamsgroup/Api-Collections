info:
  name: Remove Peer-to-Peer connection
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{BASE}}/users/peer-request/:userId/:targetId"
  params:
    - name: userId
      value: ""
      type: path
    - name: targetId
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{talent_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
