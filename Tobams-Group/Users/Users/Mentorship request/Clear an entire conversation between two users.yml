info:
  name: "Clear an entire conversation between two users "
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{BASE}}/users/messages/conversation/:otherUserId"
  params:
    - name: otherUserId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
