info:
  name: Get all saved interns
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE}}/organisation/tech-talents/bookmarks?role=intern"
  params:
    - name: role
      value: intern
      type: query
  auth:
    type: bearer
    token: "{{client_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
