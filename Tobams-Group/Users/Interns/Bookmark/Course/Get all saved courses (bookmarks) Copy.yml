info:
  name: Get all saved courses (bookmarks) Copy
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE}}/interns/courses/bookmarks"
  auth:
    type: bearer
    token: "{{intern_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
