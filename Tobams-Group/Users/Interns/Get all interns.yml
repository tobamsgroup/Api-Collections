info:
  name: Get all interns
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE}}/interns"
  auth:
    type: bearer
    token: "{{intern_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
