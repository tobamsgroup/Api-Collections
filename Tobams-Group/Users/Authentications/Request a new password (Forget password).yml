info:
  name: Request a new password (Forget password)
  type: http
  seq: 18

http:
  method: POST
  url: "{{BASE}}/auth/forgot-password"
  body:
    type: json
    data: |-
      {
           "email": "fawazmentor3@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
