info:
  name: Facebook Login
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE}}/auth/facebook"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
