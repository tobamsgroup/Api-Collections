info:
  name: Google Callback
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE}}/auth/google/callback"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
