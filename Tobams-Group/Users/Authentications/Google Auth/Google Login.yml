info:
  name: Google Login
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE}}/auth/google"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
