info:
  name: Get the top talents
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE}}/admin/top-rated-talents"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
