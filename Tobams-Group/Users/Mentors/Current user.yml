info:
  name: Current user
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE}}/mentors/me"
  auth:
    type: bearer
    token: "{{mentor_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
