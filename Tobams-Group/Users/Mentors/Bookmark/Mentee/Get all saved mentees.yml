info:
  name: Get all saved mentees
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE}}/mentors/mentees/bookmarks"
  auth:
    type: bearer
    token: "{{mentor_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
