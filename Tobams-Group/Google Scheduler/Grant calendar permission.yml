info:
  name: Grant calendar permission
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE}}/auth/google/calendar"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
