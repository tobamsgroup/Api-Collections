info:
  name: Update a course by its ID
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{BASE}}/courses/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
