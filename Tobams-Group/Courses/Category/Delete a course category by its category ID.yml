info:
  name: Delete a course category by its category ID
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{BASE}}/courses/category/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
