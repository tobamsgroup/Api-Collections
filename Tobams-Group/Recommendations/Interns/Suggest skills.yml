info:
  name: Suggest skills
  type: http
  seq: 7

http:
  method: GET
  url: "{{BASE}}/recommendations/?type=skills"
  params:
    - name: type
      value: skills
      type: query
  auth:
    type: bearer
    token: "{{intern_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
