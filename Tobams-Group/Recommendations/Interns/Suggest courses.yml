info:
  name: Suggest courses
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE}}/recommendations/?type=courses"
  params:
    - name: type
      value: courses
      type: query
  auth:
    type: bearer
    token: "{{intern_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
