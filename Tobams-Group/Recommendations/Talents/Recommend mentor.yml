info:
  name: Recommend mentor
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE}}/recommendations/?type=mentorship"
  params:
    - name: type
      value: mentorship
      type: query
  auth:
    type: bearer
    token: "{{talent_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
