info:
  name: Suggest job titles
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE}}/recommendations/?type=job_titles"
  params:
    - name: type
      value: job_titles
      type: query
  auth:
    type: bearer
    token: "{{talent_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
