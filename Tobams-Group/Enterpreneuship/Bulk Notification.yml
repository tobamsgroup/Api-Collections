info:
  name: Bulk Notification
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE}}/interest-entrepreneurship/notification?page=1&limit=20"
  params:
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "20"
      type: query
  body:
    type: multipart-form
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
