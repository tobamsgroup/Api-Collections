info:
  name: Delete an interview
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{BASE}}/interviews/:interviewId"
  params:
    - name: interviewId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
