info:
  name: Get feedback by id
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE}}/feedbacks/67c5de9ca0d16e27b7878864"
  auth:
    type: bearer
    token: "{{mentor_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
