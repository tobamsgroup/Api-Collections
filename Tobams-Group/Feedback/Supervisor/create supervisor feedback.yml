info:
  name: create supervisor feedback
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/supervisor-feedback/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
