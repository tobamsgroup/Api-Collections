info:
  name: Get webinar appilcation counts
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE}}/webinars/application-count"
  params:
    - name: status
      value: ""
      type: query
      description: " Optional `status` filter: `upcoming` (default), `completed`, `cancelled`, or `all`."
      disabled: true
    - name: page
      value: ""
      type: query
      disabled: true
    - name: limit
      value: ""
      type: query
      description: Default to 20
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Example
    request:
      url: "{{BASE}}/webinars/application-count"
      method: GET
      params:
        - name: status
          value: ""
          type: query
          description: " Optional `status` filter: `upcoming` (default), `completed`, `cancelled`, or `all`."
          disabled: true
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: |-
          {
            "success": true,
            "message": "Webinar application counts retrieved successfully",
            "data": {
              "result": [
                { "_id": "...", "title": "...", "status": "upcoming", "applicationCount": 3 }
              ],
              "pagination": {
                "page": 1,
                "limit": 20,
                "total": 1,
                "pages": 1,
                "hasNext": false,
                "hasPrev": false
              }
            }
          }
