info:
  name: Get webinar applicants
  type: http
  seq: 7

http:
  method: GET
  url: "{{BASE}}/webinar-applications/webinar/:webinarId"
  params:
    - name: webinarId
      value: 6a7c8aa857a2ff0931b00171
      type: path
  auth:
    type: bearer
    token: "{{admin_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
