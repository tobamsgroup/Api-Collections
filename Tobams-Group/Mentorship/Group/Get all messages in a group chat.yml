info:
  name: Get all messages in a group chat
  type: http
  seq: 7

http:
  method: GET
  url: "{{BASE}}/mentorship/groups/:id/messages"
  params:
    - name: id
      value: 2289999oooooosjokos
      type: path
  auth:
    type: bearer
    token: "{{intern_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
