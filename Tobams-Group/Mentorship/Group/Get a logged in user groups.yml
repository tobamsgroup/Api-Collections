info:
  name: Get a logged in user groups
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE}}/mentorship/groups/user-groups"
  auth:
    type: bearer
    token: "{{talent_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
