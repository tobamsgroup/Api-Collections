info:
  name: Get a submissions
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE}}/project-briefs/{{briefId}}"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
