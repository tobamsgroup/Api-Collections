info:
  name: Get events by title
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE}}/social_events/search/title?title=Sustainable"
  params:
    - name: title
      value: Sustainable
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
