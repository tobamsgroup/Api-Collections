info:
  name: Get All Job Applications
  type: http
  seq: 9

http:
  method: GET
  url: "{{BASE}}/job-apply/all"
  auth:
    type: bearer
    token: "{{talent_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
