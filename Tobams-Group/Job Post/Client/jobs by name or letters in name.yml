info:
  name: jobs by name or letters in name
  type: http
  seq: 7

http:
  method: GET
  url: "{{BASE}}/job_client/query/intern"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
