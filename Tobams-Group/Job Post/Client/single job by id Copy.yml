info:
  name: single job by id Copy
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE}}/job_client/id/67ed886984f868b4a93557d2"
  auth:
    type: bearer
    token: "{{talent_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
