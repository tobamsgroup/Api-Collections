info:
  name: Delete a Mentor Session
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{BASE}}/mentor_session/session/67d168bb475b00405cec62ec"
  auth:
    type: bearer
    token: "{{mentor_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
