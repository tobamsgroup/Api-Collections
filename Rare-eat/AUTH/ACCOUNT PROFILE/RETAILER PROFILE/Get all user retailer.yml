info:
  name: Get all user retailer
  type: http
  seq: 1

http:
  method: GET
  url: "{{URL}}/user/retailer/profile"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
