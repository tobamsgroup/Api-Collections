info:
  name: Delete admin Account
  type: http
  seq: 7

http:
  method: PATCH
  url: "{{URL}}/admin/auth/delete_account"
  body:
    type: json
    data: |-
      {
          "email": "abdulrasaqolarewaju88@gmail.com"
      }
  auth:
    type: bearer
    token: "{{ajwt}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
