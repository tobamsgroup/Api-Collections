info:
  name: Get Newly Created Orders
  type: http
  seq: 3

http:
  method: GET
  url: "{{URL}}/admin/order/all/newly_created"
  auth:
    type: bearer
    token: "{{ajwt}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
