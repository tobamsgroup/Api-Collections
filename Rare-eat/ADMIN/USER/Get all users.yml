info:
  name: Get all users
  type: http
  seq: 1

http:
  method: GET
  url: "{{URL}}/admin/user/all_users"
  auth:
    type: bearer
    token: "{{ajwt}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
