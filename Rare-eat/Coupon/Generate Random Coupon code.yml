info:
  name: Generate Random Coupon code
  type: http
  seq: 1

http:
  method: GET
  url: "{{URL}}/coupon/generate"
  body:
    type: text
    data: ""
  auth:
    type: bearer
    token: "{{ajwt}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
