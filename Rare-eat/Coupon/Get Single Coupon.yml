info:
  name: Get Single Coupon
  type: http
  seq: 3

http:
  method: GET
  url: "{{URL}}/coupon/single?coupon_id=abcs"
  params:
    - name: coupon_id
      value: abcs
      type: query
  body:
    type: text
    data: ""
  auth:
    type: bearer
    token: "{{jwt}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
