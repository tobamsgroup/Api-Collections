info:
  name: Get All Coupon
  type: http
  seq: 2

http:
  method: GET
  url: "{{URL}}/coupon/all"
  body:
    type: text
    data: ""
  auth:
    type: bearer
    token: "{{ajwt}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
