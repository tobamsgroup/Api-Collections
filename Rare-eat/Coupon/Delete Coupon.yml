info:
  name: Delete Coupon
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{URL}}/coupon/delete?coupon_code=SAMPLE_CODE"
  params:
    - name: coupon_code
      value: SAMPLE_CODE
      type: query
  body:
    type: text
    data: ""
  auth:
    type: bearer
    token: "{{ajwt}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
