info:
  name: Add Card Details
  type: http
  seq: 1

http:
  method: POST
  url: "{{URL}}/card/stripe/add_card"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
