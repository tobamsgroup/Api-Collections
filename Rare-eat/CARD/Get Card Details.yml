info:
  name: Get Card Details
  type: http
  seq: 2

http:
  method: GET
  url: "{{URL}}/card/stripe/user_cards"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
