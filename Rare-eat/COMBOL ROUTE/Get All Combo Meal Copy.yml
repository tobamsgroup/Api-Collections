info:
  name: Get All Combo Meal Copy
  type: http
  seq: 5

http:
  method: GET
  url: "{{URL}}/subscription/combo_meal/all"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{jwt}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
