info:
  name: CREATE PRODUCT CATEGORY
  type: http
  seq: 3

http:
  method: POST
  url: "{{URL}}/companies/product/create/categories"
  body:
    type: json
    data: "{\r\n    \"category\": \"suya\"\r\n}"
  auth:
    type: bearer
    token: "{{jwt}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
