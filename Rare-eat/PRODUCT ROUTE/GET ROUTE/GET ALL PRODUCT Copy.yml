info:
  name: GET ALL PRODUCT Copy
  type: http
  seq: 5

http:
  method: GET
  url: "{{URL}}/companies/product/search?q=jo"
  params:
    - name: q
      value: jo
      type: query
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{jwt}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
