info:
  name: GET ALL PRODUCT BY SEARCH
  type: http
  seq: 4

http:
  method: GET
  url: "{{URL}}/companies/product/all"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{jwt}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
