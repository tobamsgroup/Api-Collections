info:
  name: GET NEWLY CREATED PRODUCT
  type: http
  seq: 2

http:
  method: GET
  url: "{{URL}}/companies/product/newly_created"
  body:
    type: text
    data: ""
  auth:
    type: bearer
    token: "{{jwt}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
