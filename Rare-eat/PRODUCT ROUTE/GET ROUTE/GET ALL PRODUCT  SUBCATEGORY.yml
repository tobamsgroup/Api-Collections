info:
  name: GET ALL PRODUCT  SUBCATEGORY
  type: http
  seq: 8

http:
  method: GET
  url: "{{URL}}/companies/product/all/subcategories"
  auth:
    type: bearer
    token: "{{jwt}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
