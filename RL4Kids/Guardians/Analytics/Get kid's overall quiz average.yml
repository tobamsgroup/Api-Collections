info:
  name: Get kid's overall quiz average
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/guardian/analytics/690aa43740aedee77dee3d67/quiz-average"
  auth:
    type: bearer
    token: "{{access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
