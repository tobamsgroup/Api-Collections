info:
  name: Get Me
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/guardian/me"
  auth:
    type: bearer
    token: "{{teacher_access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
