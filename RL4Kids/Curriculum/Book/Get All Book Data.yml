info:
  name: Get All Book Data
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/book"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
