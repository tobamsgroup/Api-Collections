info:
  name: New Request
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/demo/{{id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
