info:
  name: Get Demo by ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/demo/{{id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
