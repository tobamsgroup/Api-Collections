info:
  name: Search Badges
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/badges/search?category=Learning Progress&name=Quick"
  params:
    - name: category
      value: Learning Progress
      type: query
    - name: name
      value: Quick
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
