info:
  name: Get Badges by Category
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/badges/category/Learning Progress"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
