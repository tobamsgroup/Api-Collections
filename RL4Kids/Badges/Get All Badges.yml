info:
  name: Get All Badges
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/badges"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
