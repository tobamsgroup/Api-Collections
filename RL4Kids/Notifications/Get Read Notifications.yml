info:
  name: Get Read Notifications
  type: http
  seq: 9

http:
  method: GET
  url: "{{base_url}}/notifications/read"
  auth:
    type: bearer
    token: "{{access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
