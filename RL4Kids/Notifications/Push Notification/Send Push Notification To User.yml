info:
  name: Send Push Notification To User
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/push-notifications/send-to-user/:userId"
  params:
    - name: userId
      value: 696a446b790b9d1fd444d8f8
      type: path
  body:
    type: json
    data: " {\r

      \            \"title\": \"Welcome\",\r

      \            \"message\": \"A new sustainability course has been assigned to you!\"\r

      \ }"
  auth:
    type: bearer
    token: "{{access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
