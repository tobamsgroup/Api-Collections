info:
  name: Get all push tokens for current user
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/push-tokens"
  auth:
    type: bearer
    token: "{{access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
