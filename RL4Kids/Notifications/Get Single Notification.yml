info:
  name: Get Single Notification
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/notifications/{{notification_id}}"
  auth:
    type: bearer
    token: "{{access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
