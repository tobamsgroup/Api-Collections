info:
  name: Trigger manual batch processing
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/admin-notification-batches/trigger"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
