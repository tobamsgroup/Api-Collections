info:
  name: Get Unread Notifications
  type: http
  seq: 10

http:
  method: GET
  url: "{{base_url}}/notifications/unread"
  auth:
    type: bearer
    token: "{{access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
