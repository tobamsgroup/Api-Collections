info:
  name: Get All Notifications
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/notifications"
  auth:
    type: bearer
    token: "{{access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
