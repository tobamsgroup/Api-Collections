info:
  name: Delete Single Application
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/teach/{{id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
