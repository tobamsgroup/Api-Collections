info:
  name: Get all Avatars
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/upload/avatars"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
