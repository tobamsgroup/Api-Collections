info:
  name: Get Recent Activities
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/activities/recent"
  auth:
    type: bearer
    token: "{{access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
