info:
  name: Get an Activity
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/activities/646e1234f12345678901234"
  auth:
    type: bearer
    token: "{{access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
