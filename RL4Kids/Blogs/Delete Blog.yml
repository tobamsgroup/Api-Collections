info:
  name: Delete Blog
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{base_url}}/blog/{{id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
