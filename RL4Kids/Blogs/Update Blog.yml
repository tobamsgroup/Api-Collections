info:
  name: Update Blog
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{base_url}}/blog/{{id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
