info:
  name: Get Latest Blogs
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/blog/latest"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
