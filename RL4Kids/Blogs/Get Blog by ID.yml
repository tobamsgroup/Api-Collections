info:
  name: Get Blog by ID
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/blog/{{id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
