info:
  name: Logout a user
  type: http
  seq: 7

http:
  method: POST
  url: "{{base_url}}/auth/logout"
  auth:
    type: bearer
    token: "{{access}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
