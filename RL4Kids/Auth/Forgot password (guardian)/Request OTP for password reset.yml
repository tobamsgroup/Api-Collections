info:
  name: Request OTP for password reset
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/auth/forgot-password"
  body:
    type: json
    data: |-
      {
          "email": "rolatzarza@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
